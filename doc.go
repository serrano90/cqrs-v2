// Package cqrs provides generic (type-safe) interfaces to structure an
// application using CQRS (Command Query Responsibility Segregation):
// commands, queries, events, their handlers, aggregates, and the
// publish/subscribe event bus contract.
package cqrs
