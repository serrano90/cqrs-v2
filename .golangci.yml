version: "2"

linters:
  # standard enables errcheck, govet, ineffassign, staticcheck, and unused.
  default: standard
  enable:
    - misspell
    - revive

formatters:
  enable:
    - gofmt
    - goimports
